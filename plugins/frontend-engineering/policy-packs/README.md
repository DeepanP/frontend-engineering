# Policy Packs

Policy packs are reusable engineering standards.

Current bootstrap can discover available packs and generate
`.github/copilot/project-policy.json`.

Future versions will recommend packs automatically using workspace detectors.
